{% extends 'base.html.twig' %}

{% block title %}Nos biens
{% endblock %}

{% block content %}

	{% if block ('search') is defined %}{% endif %}

	<div class="row">

		{% for product in products %}
			<div class="col-md-4 col-sm-12">
				<div class="card" style="width: 18rem;">
					<img src="/uploads/{{ product.picture }}" class="card-img-top" alt="{{ product.name}}" class=" img-fluid">
					<div class="card-body">
						<p class="card-text">
							{{ product.name}}</p>
						<p class="card-text">
							{{ product.price / 100 }}
							€</p>

						<p class="card-text">{{ product.aera}}
							m2</p>
					</div>
				</div>
			</div>

		{% endfor %}
	</div>
{% endblock %}
